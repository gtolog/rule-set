name: convert-list-to-sys

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["resolve-DoH-elf-hosts-and-save-ipcidr"]  # Name of the first workflow
    types:
      - completed

env:
  LIST-TO-BE-CONVERTED: "https://raw.githubusercontent.com/gtolog/rule-set/refs/heads/main/DoH-elf-host.list"

jobs:
  convert-list-to-sys:
    runs-on: ubuntu-latest

    steps:
    - name: Set up Python3
      uses: actions/setup-python@v2
      with:
        python-version: 3.x
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas requests pyyaml

    - name: "Setup sing-box"
      env:
        SING_BOX_DEB_URL: "https://github.com/SagerNet/sing-box/releases/download/v1.11.7/sing-box_1.11.7_linux_amd64.deb"
      run: |
          set -Eeuo pipefail
          wget -O sing-box.deb $SING_BOX_DEB_URL
          sudo dpkg -i sing-box.deb

    - name: Checkout repository
      uses: actions/checkout@v2
        
    - name: convert list
      run: python converter.py

    - name: commit and push
      run: |
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action"
        git add *.json
        git add *.srs

        if git diff --staged --quiet; then
          echo "No changes to commit"
          exit 0
        else
          git commit -m "Update rules"
          git push
        fi
